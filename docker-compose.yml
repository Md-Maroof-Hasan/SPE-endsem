services:

  auth-service:
    build: ./auth-service

  notes-service:
    build: ./notes-service
    depends_on:
      - auth-service
      - audit-service

  audit-service:
    build: ./audit-service

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - auth-service
      - notes-service
      - audit-service
